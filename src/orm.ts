import mongoose from "mongoose";

import { Posts } from "./posts";

export const insertPost = async (

    titolo: string,
    anno : number,
    descrizione: string,
    valutazione: string
) => {
    
    
}
